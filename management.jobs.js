var jobBuild = require('job.build');
var jobHarvest = require('job.harvest');
var jobUpgrade = require('job.upgrade');

var managementJobs = {
    startWork: function (creep){
        
    }
};
module.exports = managementJobs;